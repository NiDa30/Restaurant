name: restaurant
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 # Updated version of http
  provider: ^6.1.4 # Updated version of provider
  cupertino_icons: ^1.0.8
  sqflite: ^2.0.0 # Updated version of sqflite
  path_provider: ^2.0.11 # Updated version of path_provider
  shared_preferences: ^2.0.13 # Updated version of shared_preferences

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/test.json
    - assets/images/
